/**
 * e.target.email.value || with the help of input field
 * use form action and formData in the action handler formData.get("email")
 * Controlled component. One per each filed. Use state on change of the field. use to dynamic handle error
 * handle all controlled filed on one state object
 * Uncontrolled file
 * Custom hook to collect form data with useInputFiled
 */
